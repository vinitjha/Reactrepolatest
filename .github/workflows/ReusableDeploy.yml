name: ReusableDeploy

on:
  workflow_call:
    inputs:
      environment:
        required: false
        type: string
        default: "dev"
      outputs:
        result:
          description: The result of the deployment automation
          value: 
          
jobs:
  deploy:
   outputs:
     outcome: ${{ steps.set-result.outputs.step-result }} 
   
   runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: |
          echo "Deploying & Uploading.."
          echo "Environment: ${{ inputs.environment }}"


